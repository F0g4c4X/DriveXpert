<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DriveXpert - Detalhes do Carro</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        .details-container {
            max-width: 900px;
            margin: 40px auto;
        }
        .info-card {
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0,0,0,0.1);
            margin-bottom: 20px;
        }
        .evaluation-status {
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 20px;
        }
        .evaluation-item {
            border-left: 3px solid #007bff;
            padding-left: 15px;
            margin-bottom: 15px;
        }
    </style>
</head>
<body class="bg-light">
    <div class="container details-container">
        <nav aria-label="breadcrumb" class="mb-4">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/dashboard">Dashboard</a></li>
                <li class="breadcrumb-item"><a href="/cars/my">Meus Carros</a></li>
                <li class="breadcrumb-item active">Detalhes</li>
            </ol>
        </nav>

        <div class="card info-card">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-start mb-4">
                    <div>
                        <h2 class="mb-1"><%= car.marca %> <%= car.modelo %></h2>
                        <p class="text-muted mb-0">Ano <%= car.ano %></p>
                    </div>
                    <% if (car.status === 'pendente') { %>
                        <span class="badge bg-warning">Pendente</span>
                    <% } else if (car.status === 'avaliado') { %>
                        <span class="badge bg-success">Avaliado</span>
                    <% } else { %>
                        <span class="badge bg-primary">Vendido</span>
                    <% } %>
                </div>

                <div class="row">
                    <div class="col-md-6">
                        <h5>Informações do Veículo</h5>
                        <ul class="list-unstyled">
                            <li class="mb-2">
                                <i class="bi bi-speedometer2"></i>
                                <strong>Kilometragem:</strong> <%= car.kilometragem.toLocaleString() %> km
                            </li>
                            <li class="mb-2">
                                <i class="bi bi-tag"></i>
                                <strong>Placa:</strong> <%= car.placa %>
                            </li>
                            <li class="mb-2">
                                <i class="bi bi-palette"></i>
                                <strong>Cor:</strong> <%= car.cor %>
                            </li>
                            <li class="mb-2">
                                <i class="bi bi-fuel-pump"></i>
                                <strong>Combustível:</strong> <%= car.combustivel.charAt(0).toUpperCase() + car.combustivel.slice(1) %>
                            </li>
                        </ul>
                    </div>
                    <div class="col-md-6">
                        <h5>Informações do Proprietário</h5>
                        <ul class="list-unstyled">
                            <li class="mb-2">
                                <i class="bi bi-person"></i>
                                <strong>Nome:</strong> <%= car.proprietario.name %>
                            </li>
                            <li class="mb-2">
                                <i class="bi bi-envelope"></i>
                                <strong>Email:</strong> <%= car.proprietario.email %>
                            </li>
                        </ul>
                    </div>
                </div>

                <% if (car.observacoes) { %>
                    <div class="mt-3">
                        <h5>Observações</h5>
                        <p class="mb-0"><%= car.observacoes %></p>
                    </div>
                <% } %>
            </div>
        </div>

        <% if (car.evaluation) { %>
            <div class="card info-card">
                <div class="card-body">
                    <h4 class="mb-4">Avaliação Técnica</h4>
                    
                    <div class="row mb-4">
                        <div class="col-md-6">
                            <h5>Preço Avaliado</h5>
                            <h3 class="text-primary">R$ <%= car.evaluation.precoAvaliado.toLocaleString() %></h3>
                        </div>
                        <div class="col-md-6">
                            <h5>Estado Geral</h5>
                            <h3><%= car.evaluation.estadoGeral %></h3>
                        </div>
                    </div>

                    <h5 class="mb-3">Itens Avaliados</h5>
                    <div class="evaluation-item">
                        <h6>Motor</h6>
                        <p class="mb-1"><strong>Estado:</strong> <%= car.evaluation.itensAvaliados.motor.estado %></p>
                        <p class="text-muted mb-0"><%= car.evaluation.itensAvaliados.motor.observacao %></p>
                    </div>

                    <div class="evaluation-item">
                        <h6>Carroceria</h6>
                        <p class="mb-1"><strong>Estado:</strong> <%= car.evaluation.itensAvaliados.carroceria.estado %></p>
                        <p class="text-muted mb-0"><%= car.evaluation.itensAvaliados.carroceria.observacao %></p>
                    </div>

                    <div class="evaluation-item">
                        <h6>Interior</h6>
                        <p class="mb-1"><strong>Estado:</strong> <%= car.evaluation.itensAvaliados.interior.estado %></p>
                        <p class="text-muted mb-0"><%= car.evaluation.itensAvaliados.interior.observacao %></p>
                    </div>

                    <div class="evaluation-item">
                        <h6>Documentação</h6>
                        <p class="mb-1"><strong>Estado:</strong> <%= car.evaluation.itensAvaliados.documentacao.estado %></p>
                        <p class="text-muted mb-0"><%= car.evaluation.itensAvaliados.documentacao.observacao %></p>
                    </div>

                    <% if (car.evaluation.observacoes) { %>
                        <div class="mt-4">
                            <h5>Observações do Avaliador</h5>
                            <p class="mb-0"><%= car.evaluation.observacoes %></p>
                        </div>
                    <% } %>
                </div>
            </div>
        <% } else if (car.status === 'pendente') { %>
            <div class="evaluation-status bg-light">
                <div class="text-center">
                    <i class="bi bi-clipboard-check display-4 text-muted"></i>
                    <h4 class="mt-3">Aguardando Avaliação</h4>
                    <p class="text-muted">Este veículo ainda não foi avaliado por um de nossos especialistas.</p>
                </div>
            </div>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 